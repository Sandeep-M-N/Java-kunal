package OOPS.polymorphism;

public   class Shapes {
    // when a class is declared as final the other class cannot inherit or extends this class.
    // u cannot override a method when it is declared as final.
   void area(){
        System.out.println("area of which");
    }
}
